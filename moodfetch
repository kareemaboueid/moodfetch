#!/usr/bin/env bash
# vim: noai:ts=4:sw=4:expandtab
# shellcheck source=/dev/null
# shellcheck disable=2009
#
# moodfetch - ((experimental))
#
# @author Kareem Aboueid
# Moodfetch: A command-line tool for displaying your system's mood.
# https://github.com/kareemaboueid/moodfetch
#
# Hi, This is an ((experimental)) project for fetching and displaying system "mood"
# based on various system metrics, such as CPU usage, memory usage,
# disk space, and network activity, in a visually (sarcastic) manner,
# using ASCII art (emojis for now) and colors, somehow similar to neofetch but dumber.

readonly proc_uptime_path="/proc/uptime"
readonly battery_base="/sys/class/power_supply/BAT0"
readonly battery_capacity_path="${battery_base}/capacity"
readonly battery_status_path="${battery_base}/status"

read_system_uptime_hours_integer() {
  local seconds_float
  seconds_float="$(awk '{print $1}' "${proc_uptime_path}")"
  awk -v secs="${seconds_float}" 'BEGIN { printf("%d\n", int(secs/3600)) }'
}

read_battery_percentage_if_available() {
  if [ -r "${battery_capacity_path}" ]; then
    cat "${battery_capacity_path}"
  else
    printf ""
  fi
}

is_battery_currently_plugged_in() {
  if [ -r "${battery_status_path}" ]; then
    grep -qE "Charging|Full" "${battery_status_path}" && return 0 || return 1
  fi
  return 1
}

choose_mood_message_two_line() {
  local uptime_hours="$1"
  local battery_pct="$2"
  local battery_plugged_flag="$3"

  if [ -n "${battery_pct}" ] && [ "${battery_pct}" -lt 20 ] && [ "${battery_plugged_flag}" -ne 0 ]; then
    echo "🔋"
    echo "Battery at ${battery_pct}% — bold of you to trust unsaved work."
    return
  fi

  if [ "${uptime_hours}" -gt 48 ]; then
    echo "🧟"
    echo "Uptime ${uptime_hours}h — I’m basically a night-shift barista. Reboot therapy?"
    return
  fi

  echo "😏"
  echo "Everything’s suspiciously fine — which statistically means chaos is one tab away."
}

main() {
  local system_uptime_hours
  system_uptime_hours="$(read_system_uptime_hours_integer)"

  local battery_percentage
  battery_percentage="$(read_battery_percentage_if_available)"

  local not_plugged=1
  if is_battery_currently_plugged_in; then
    not_plugged=0
  else
    not_plugged=1
  fi

  choose_mood_message_two_line "${system_uptime_hours}" "${battery_percentage}" "${not_plugged}"
}

main "$@"